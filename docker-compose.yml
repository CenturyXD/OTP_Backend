version: "3.8"
services:
  db_otp:
    image: postgres:15
    container_name: db_otp
    restart: on-failure
    ports:
      - "5432:5432"
    volumes:
      - db_otp_data:/var/lib/postgresql/data
    networks:
      - app_network
    environment:
      - POSTGRES_DB=db_otp
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres

  app:
    build:
      context: ./backend
    container_name: laravel_app_otp
    restart: on-failure
    volumes:
      - ./backend:/var/www/html
      - ./backend/storage:/var/www/html/storage
      - ./backend/bootstrap/cache:/var/www/html/bootstrap/cache
      - /var/www/html/vendor
    networks:
      - app_network
    environment:
      - APP_NAME=Laravel
      - APP_ENV=local
      - APP_KEY=base64:Fz9MpgEpswCWwXBBJpM86y+b1tMWnGMTsFACdAzbJb8=
      - APP_DEBUG=true
      - APP_URL=http://localhost
      - DB_CONNECTION=pgsql
      - DB_HOST=db_otp
      - DB_PORT=5432
      - DB_DATABASE=db_otp
      - DB_USERNAME=postgres
      - DB_PASSWORD=postgres
    ports:
      - "8000:8000"
    depends_on:
      - db_otp

networks:
  app_network:
    driver: bridge

volumes:
  db_otp_data: